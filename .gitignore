.idea/
.DS_Store

# Go build
/dist/

# caddie managed, local and per-machine.
# These four strings must match the entries in ensureProjectGitignore
# (cmd/caddie/main.go) exactly, or caddie appends a duplicate block.
.claude/skills/
.agents/skills/
.claude/.caddie-fingerprint
.caddie.yaml

# git worktrees created by tooling
.claude/worktrees/
